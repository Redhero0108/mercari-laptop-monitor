# 结果表清晰度改造实施计划

1. 在 `test-results-page.mjs` 中先加入排序文案、结构化参数、七列表格和恢复按钮的断言，并确认测试失败。
2. 在 `results-page.mjs` 中新增纯函数生成排序文案与结构化商品参数。
3. 更新结果行、表头、工具栏和内嵌脚本：移除逐行检查时间，显示当前排序，支持恢复输出顺序。
4. 更新 CSS 的列宽、响应式隐藏和排序状态控件。
5. 运行完整测试与语法检查，生成 `results.html`，在真实浏览器验证排序、恢复、搜索筛选兼容性和七列布局。
6. 重启现有后台监测进程，使后续生成页面继续使用新模板，然后提交变更。
